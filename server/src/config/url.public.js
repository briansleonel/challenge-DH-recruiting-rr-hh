const URL = "http://localhost:3004";

const URL_API = `${URL}/api`;
const URL_PUBLIC_IMAGES_ASPIRANTES = `${URL}/img/aspirantes`;

module.exports = {
    URL,
    URL_API,
    URL_PUBLIC_IMAGES_ASPIRANTES,
};